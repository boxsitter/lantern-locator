# direnv configuration — automatically loads the Nix dev environment
# when you cd into this directory.
# REQUIREMENTS:
#   - flake.nix must exist in this directory
#   - requirements.txt should list locked (pinned) dependencies

# Load the Nix flake environment (shellHook handles all validation and setup)
use flake
